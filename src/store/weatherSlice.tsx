import { createAsyncThunk, createSlice } from '@reduxjs/toolkit'

// export const fetchWeather = createAsyncThunk<WeatherPayload>()

export const weatherSlice = createSlice({
  name: 'weather',
  initialState: {} as any,
  reducers: {},
  extraReducers: {}
})
